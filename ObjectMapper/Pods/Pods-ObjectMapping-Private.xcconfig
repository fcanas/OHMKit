#include "Pods-ObjectMapping.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/ObjectMapping" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/ObjectMapping"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}